# Buổi 1 – Key takeaways

- Khởi tạo project Playwright (`npm init playwright@latest` hoặc tương đương).
- Cấu trúc cơ bản: `tests/`, `playwright.config.ts`, `package.json`.
- Chạy test: `npx playwright test`.
- Cấu hình Git và đẩy code lên GitHub (init project, remote, push).
